
import { CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export const SubscriptionHeader = () => {
  return (
    <CardHeader>
      <CardTitle>訂閱狀態</CardTitle>
      <CardDescription>目前方案與到期時間</CardDescription>
    </CardHeader>
  );
};
