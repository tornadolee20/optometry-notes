# 2026-08-26

- 完成 `KDF_CROSS_NODE_DISCOVERY_V0_1`：deterministic、local、read-only、candidate-only，含 Discovery Lab、Graph temporary overlay、Mandala/Ask KDF 鬆耦合入口與 focused tests。
- Formal KDF 封口仍為 22 artifacts、186 wikilinks、PASS、digest `df3d6cff18ab22b8120dabf80fc3feb964a644fb40a2cedf1e4cfa5ed939f398`；Legacy 69 篇與 Bridge audit 均未變。
- 自我修正：瀏覽器控制層沒有 `waitForSelector`／tab-level `setViewportSize`；應使用 locator `waitFor`，指定尺寸則先取得 browser `viewport` capability。1440×1000 驗收曾發現長標題橫向溢位，補上 `min-width: 0` 與 `overflow-wrap: anywhere` 後重驗通過。
