# 2026-04-15 optometry writer legacy

- Added `skills/optometry-writer/LEGACY-MIGRATION.md`.
- New default routing should prefer `uncle-glasses-blog-packager` for publish-layer tasks.
- `optometry-writer` should now be treated as source material / fallback context until a renderer-only replacement is extracted.
