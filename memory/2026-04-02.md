# 2026-04-02 工作日誌

## 09:00 — MYOWNREVIEWS 技術債清除

### 完成項目

**推薦碼系統**
- `Register.tsx`：Step 3 新增推薦碼輸入欄（可選填，格式驗證）
- `StoreProfile.tsx`：新增推薦統計卡片（已推薦/已訂購/試用中 3 欄）

**AI 產業關鍵字生成（Edge Function 升級）**
- `generate-template-keywords`：加入 8 個產業特化研究框架（視光/醫療/牙科/美容/餐飲/教育/健身/寵物）
- 初次請求 60 個、過濾取最佳 48；智慧補足（針對不足構面）；均衡分布修剪
- 字數計算改用 `[...str].length`（修正中文計算 bug）
- `generate-brand-keywords`：同步套用新策略，初次 60 個、智慧補足針對不足 dimension

**共用模組**
- 建立 `supabase/functions/_shared/openai.ts`（corsHeaders、callOpenAI、charCount）
- 兩個 Edge Function 都改用共用模組，消除重複程式碼

**前端優化**
- `IndustryTemplatesTab`：用 `useQuery` + `useQueryClient.invalidateQueries` 取代手動 `loadTemplates`（快取 30 秒）
- 批量生成：串行改 3 並發，失敗模板會記錄並顯示在 Toast
- `parentLabel` 修正：查找父分類名稱傳入 Edge Function，不再傳自己當自己的父分類
- `KeywordDraftPreview`：字數驗證改用 `[...str].length`

**TypeScript 型別**
- 重新生成 `types.ts`，`referred_by_code` 現在有正確型別（不再是 `any`）

### 部署
- `generate-template-keywords` ✅ 部署到 `wfaqnahahygtieyjnlji`
- `generate-brand-keywords` ✅ 部署到 `wfaqnahahygtieyjnlji`

### 待跟進
- 跑 `npm run build` 確認 TypeScript 無新錯誤
- 進後台執行一次批量生成，填滿所有產業模板關鍵字
- 推薦碼端對端測試（新店家註冊 → StoreProfile 統計更新）
