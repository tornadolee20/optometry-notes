# 2026-08-23

## KDF Bridge v0.1.2 operational closure

- Added deterministic current-Vault snapshot and explicit, human-gated baseline
  promotion while preserving the immutable 17-card v0.1 baseline.
- Completed request-level idempotency, discriminated tool schemas, expiring private
  prepared state, redacted audit retention, Windows Node.js 22 CI, and failure drills.
- Final local evidence: build PASS; 31/31 tests PASS; live validator 18 artifacts,
  172 Wikilinks, 0 errors, 0 warnings; three snapshot runs produced
  `0a89900ee850fd816d5c3c97ca76da8ad0ee8ec5f05000e868b6746770980323`;
  `git diff --check` PASS.
- Actual ChatGPT frontend Golden Path is `READY BUT NOT EXECUTED`.
- The read-only `.agents/workflows/knowledge-discovery.md` still references the
  removed Python validator. Replace it with the Node CLI command when that protected
  path is owner-writable.

## Self-correction

- Plain `npm.cmd ci` hit an execution-sandbox permission error in the global npm
  cache. Using the repo-local `.npm-cache` completed the same lockfile install without
  changing dependency scope. Prefer a repo-local npm cache in restricted Windows
  verification environments.

## Operational acceptance continuation

- Re-ran the required local closure sequence: dependency install, build, 31/31 tests,
  validator, three identical snapshots, formal baseline check, diff check, and
  residue/privacy checks all passed.
- AC-15 remains `NOT VERIFIED`: the only executable workflow reference to the removed
  Python validator is in `.agents/workflows/knowledge-discovery.md`, and the protected
  reparse-point boundary rejected a one-line `apply_patch`. No permission bypass was
  attempted.
- Expanded `CHATGPT-E2E-v0.1.2.md` with executable prompts, expected modes/results,
  state/no-change checks, a structured evidence table, missing-prepared recovery, and
  the non-blocking natural-language Uncle Test. Actual ChatGPT execution remains
  `READY BUT NOT EXECUTED`.
- GitHub Windows workflow remains runner-unverified because it is intentionally
  uncommitted and the configured `gh` account token is invalid. No commit, push,
  merge, or workflow trigger was attempted.
- Patch-tool lesson: replacing one file with both delete and add in a single patch is
  rejected; use separate patch operations while preserving the intended file content.
- PowerShell lesson: do not pipe directly from a statement-form `foreach` block in
  this host; accumulate objects into an array and pipe the array to `ConvertTo-Json`.
