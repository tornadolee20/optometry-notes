# 2026-07-11

## Self-correction: Sites static preview returned 404

- Mistake: treated a successful Sites deployment status as proof that the deployed routes and assets were reachable.
- Cause: the minimal worker delegated to an `ASSETS` binding that the saved-version runtime did not provide; the first repair only remapped `/` and did not verify every deployed asset.
- Workflow correction: after every Sites deployment, verify `/`, CSS, JavaScript, data, and representative media URLs with authenticated requests before reporting a preview as ready.
