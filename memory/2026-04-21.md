# 2026-04-21

## Self-Correction Log

- 安裝 `alchaincyf/huashu-design` skill 時，直接執行 `npx skills add ...` 先被 PowerShell execution policy 擋下，因為它會呼叫 `npx.ps1`。
- 修正做法：在 Windows PowerShell 環境中改用 `npx.cmd skills add ...`。若遇到 npm registry / cache 的 `EACCES`，依權限流程提權重跑。
- 後續慣例：執行 npm/npx 類命令時，若在 PowerShell 看到 `.ps1` policy 錯誤，優先改走 `.cmd` 入口，不要把它誤判成套件本身故障。

## Skill Install

- 已安裝 `huashu-design` 到 `.agents/skills/huashu-design`。
- 安裝輸出顯示 universal 包含 Antigravity、Codex、Gemini CLI 等，並 symlink 給 Claude Code。

## Skill Install

- Installed Claude Code skill `bazi` from https://github.com/jinchenma94/bazi-skill into `.claude/skills/bazi`.
- Note: Codex `skill-installer` helper rejected repo root path `.` as an invalid skill name, so installation followed the repo README via `git clone`.
- Verification: `.claude/skills/bazi/SKILL.md` and `.claude/skills/bazi/references/` exist.
- Caveat: running git inside `.claude/skills/bazi` from Codex sandbox reports `dubious ownership`; this does not prevent Claude Code from loading the skill, but git operations there may need `safe.directory`.

## Skill Install

- Installed Numerologist skills from https://github.com/FANzR-arch/Numerologist_skills into `.claude/skills/`.
- Verified installed/available skill entrypoints: `.claude/skills/qimen-dunjia/SKILL.md`, `.claude/skills/ziwei-doushu/SKILL.md`, `.claude/skills/bazi/SKILL.md`.
- `qimen-dunjia` includes `references/` and `scripts/`; `ziwei-doushu` and `bazi` include `references/`.
- Note: install command exceeded timeout after clone output, but follow-up verification confirmed skill files are present and temp clone was cleaned up.

## Plugin Install

- Installed Claude Code plugin `AK-Threads-booster` from https://github.com/akseolabs-seo/AK-Threads-booster into `.claude/plugins/AK-Threads-booster`.
- Official `claude install-plugin ...` could not run because `claude` CLI was not found in this environment.
- Manual clone verification passed for `.claude-plugin`, `SKILL.md`, `skills/setup/SKILL.md`, `skills/topics/SKILL.md`, and `scripts/`.
