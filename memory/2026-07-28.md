# 2026-07-28

## 公司電腦與個人筆電共用記憶

- 確認 `optometry-notes` 為兩台電腦共用的工作與記憶核心。
- GitHub repository `tornadolee20/optometry-notes` 為跨電腦傳輸層。
- `Inbox/` 保存原始收集，`memory/` 保存工作記憶，`MEMORY.md` 與 `obsidian-vault/` 保存長期記憶。
- `LOCAL-ROLE.md` 與 `.agent-memory/` 維持每台機器本機專用，不作為跨電腦真相來源。
- 公司電腦角色為 `company-worker`；個人筆電預設為 `personal-primary`。
- Claude Code、Codex、Antigravity、Hermes 共用同一套 repository 記憶與 handoff 規則。
- Codex 與 Hermes 已在公司電腦連接 `uncle-glasses` 與 `uncle-glasses-obsidian` MCP。
- Claude Code 已安裝，專案 MCP 已辨識，尚需使用者首次啟動時批准 `.mcp.json`。

## 驗證

- Codex user config 已列出兩個 MCP。
- Hermes `mcp test` 已成功發現 7 個工具。
- `.mcp.json`、Hermes YAML、PowerShell 啟動與同步腳本均通過語法檢查。
- 同步腳本遇到未提交工作會停止，避免跨電腦覆蓋。

## 下一步

- 將整合 commit 推送至 GitHub。
- 個人筆電拉取整合分支後執行 `scripts/setup-workstation.ps1 -Role PersonalPrimary`。
- 個人筆電首次啟動 Claude Code 時批准兩個專案 MCP。
