# 2026-08-24

## Self-correction

- `rg.exe` failed to start from the WinGet link with a Windows file-association error while searching the workspace. Use `git grep` for tracked files or PowerShell `Select-String` as the local fallback; do not treat the empty search as evidence that no match exists.
