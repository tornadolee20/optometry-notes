# 2026-04-16

## Knowledge compiler upgrade

- Promoted the workspace from "shared memory" toward "compiled knowledge."
- Added `KNOWLEDGE-COMPILER-PROTOCOL.md` to define the shift from temporary retrieval to persistent wiki maintenance.
- Added `.agents/workflows/knowledge-compiler.md` as the operational workflow:
  `ingest -> compile -> query -> file-back -> lint`.
- Added Obsidian SOP note:
  `obsidian-vault/05-營運SOP與模板/SOP-知識編譯工作流.md`.
- Added reusable template:
  `obsidian-vault/06-模板 (Templates)/知識編譯頁-WikiNote.md`.
- Main design decision:
  raw sources remain preserved, while every meaningful read should cause a durable wiki change.
- Practical rule:
  prefer updating existing cards and MOCs before creating new notes.

## 01:30 — Antigravity 工作區重構

### 問題根源
optometry-notes 有 37,930 個檔案（含 saas_sandbox 360MB、AI之眼 323MB），
Antigravity 索引時超時，導致任務卡在 Generating 永不完成。
MCP broken servers（dart-mcp-server、StitchMCP）是次要問題，已清除。

### 修復紀錄
- `mcp_config.json` 清空（移除兩個壞掉的 MCP server）
- `skills.txt` 路徑從 Dropbox 修正為 Desktop
- `git safe.directory` 補上 `C:/src/flutter`（dart 帳號所有權問題）
- `storage.json` 清除重複的 Dropbox / home-dir workspace 條目
- `.gitignore` 補上 `saas_sandbox/`、`AI之眼/`、`projects/`
- `.vscode/settings.json` 新增 `files.exclude` 排除大型非視光目錄
- Antigravity Settings → Review Policy 改為 `Always Proceeds`

### 架構決策：ag-workspace
建立 `C:\Users\torna_3j3fz9h\Desktop\ag-workspace\` 作為 Antigravity 專用輕量入口。

| 內容 | 類型 |
|------|------|
| `Inbox/` | Junction → optometry-notes/Inbox |
| `drafts/` | Junction → optometry-notes/drafts |
| `memory/` | Junction → optometry-notes/memory |
| `skills/` | Junction → optometry-notes/skills |
| 協定文件 | 靜態複製（MEMORY.md、prompt-antigravity.md 等） |

### 各 Agent 工作目錄
| Agent | 開哪個資料夾 |
|-------|-------------|
| Claude Code | `optometry-notes`（完整） |
| Codex | `optometry-notes`（完整） |
| Antigravity | `ag-workspace`（輕量） |
| Obsidian | `optometry-notes/obsidian-vault` |

### 待注意
協定文件（MEMORY.md 等）是靜態複製，若 optometry-notes 有更新需手動同步到 ag-workspace。
日後考慮改用 Junction 或自動同步腳本。

---

## 03:00 — Telegram Bot 全自動系統建立完成

### 建立的檔案
- `ag-workspace/telegram-bot.js` — 主 Bot（v2.2）
- `ag-workspace/start-bot.bat` — 雙擊啟動捷徑
- `ag-workspace/install-startup.ps1` — 開機自動啟動安裝腳本
- `ag-workspace/.env` — 含 TELEGRAM_BOT_TOKEN + GEMINI_API_KEY

### Bot 技術規格
- 模型：Gemini 2.5 Flash（`thinkingBudget: 0`，關閉思考模式）
- 通訊：Telegram 長輪詢（30s timeout）
- Bot 帳號：@Uncleglassesbot
- 使用者 chat ID：8005743631

### 支援指令（中英文皆可）
| 指令 | 中文別名 | 功能 |
|------|---------|------|
| `/task` | `/任務` `/待辦` `/交辦` | 建立待辦，加 `[待 Claude 處理]` |
| `/inbox` | `/收件箱` `/收集` | 寫入手機收集箱 |
| `/draft` | `/草稿` `/文章` | Gemini 起草大綱，存至 drafts/ |
| `/memory` | `/記憶` `/今日` | 查今日工作紀錄 |
| `/status` | `/狀態` `/進度` | Inbox 待處理數量 |

### 記憶整合
- 所有對話自動寫入 `memory/YYYY-MM-DD.md`（Junction → optometry-notes）
- `/task` 寫入 `[待 Claude 處理]` 標記，Claude Code 啟動時自動接手
- `/inbox` 寫入 `Inbox/手機收集箱.md`（Junction → optometry-notes）
- Bot 讀取 MEMORY.md + 今日 memory + Inbox 作為上下文

### 架構決策
- Bot 只跑在診所/家用電腦，公司電腦不需要
- Antigravity mcp_config.json 已清空（telegram-mcp-bot 會衝突）
- Gemini API key 存在 ag-workspace/.env，免費額度每天 1500 次請求
- Token 費用：Gemini（Bot）與 Claude（Claude Code）完全獨立，互不影響

### 待升級項目（用戶已確認有興趣）
- [ ] Bot 整合 Obsidian 知識庫搜尋（回答時參考知識卡片）
- [ ] Bot 整合 uncle-glasses-mcp SaaS 資料庫查詢
- [ ] 開機自動啟動：執行 `install-startup.ps1`（尚未執行）

### 三 Agent 分工確認
| Agent | 入口 | 職責 |
|-------|------|------|
| Telegram Bot | 手機 | 即時問答、任務收集、草稿起草 |
| Antigravity | ag-workspace | 網頁操作、批次預處理 |
| Claude Code（我） | optometry-notes | 深度工程、知識整合、腳本執行 |
