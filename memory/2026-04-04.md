# 2026-04-04 日誌

## 14:xx — Claude Plugin 升級計畫：層 2 + 層 3 完成

### 層 2 — Claude Plugin 打包（完成）
建立 `plugins/claude/uncle-glasses/` 完整 plugin 結構：
- `.claude-plugin/plugin.json`：plugin 宣告
- `commands/ai-eye.md`、`commands/cast.md`、`commands/inbox.md`：slash commands
- `agents/optometry-writer.md`、`agents/literature-scout.md`：專用 agents（model: sonnet）
- `skills/paper-triage/SKILL.md`、`skills/article-draft/SKILL.md`：自動觸發 skills

### 層 3 — 本地 MCP Server（完成，測試通過）
建立 `mcp-server/uncle-glasses/`：
- `index.js`：Node.js MCP server（@modelcontextprotocol/sdk）
- `package.json` + `node_modules`：依賴已安裝
- 4 個工具：
  - `search_knowledge_cards(query)` → 搜 `04-知識卡片/`（測試：近視查詢回傳 18 張卡片）
  - `get_article_draft(topic)` → 搜 `content-planning/`
  - `list_published_articles(keyword?)` → 列 `10-歷史文章智庫/`
  - `search_literature(keyword)` → 搜 `02-文獻與期刊/`

### 啟動設定
- `.claude/settings.json`：已寫入 mcpServers 設定，重啟 Claude Code 即自動載入
- `plugins/claude/uncle-glasses/.mcp.json`：plugin 層 MCP 指標

### 重要發現：Jarvis 昨晚也建了 MCP Server
- `mcp-servers/uncle-glasses-mcp/`（TypeScript 版，Jarvis 建）
- 工具：`search_obsidian` + `create_obsidian_card`（可直接寫入 Obsidian）
- 已測試成功：以「散瞳劑」搜尋、自動建立知識卡片

### 整合結果
- `.claude/settings.json` 同時註冊兩個 MCP server
- 兩者互補：Claude版負責分類搜尋，Jarvis版負責寫入Obsidian

### Git
- commit: `cd6d2c4`
- push: ✅ 推上 GitHub tornadolee20/optometry-notes

### 待辦
- MCP 工具仍未載入（兩次重啟皆未出現），已將設定移至 user-level `C:/Users/w7/.claude/settings.json`，待第三次重啟確認

---

## 下午 — 內容缺口分析 + 高齡駕駛文章產出

### 內容缺口分析
盤點 65 篇歷史文章 + 選題庫，識別主要缺口：
- **P0 缺口**：消費決策流程全系列（配鏡流程、驗光所 vs 眼鏡行、鏡框選購、鏡片品牌比較）— 完全空白
- **P0 缺口**：角膜塑型片完整衛教、阿托品家長指南
- **P1 缺口**：飛蚊症、弱視、白內障/AMD 專文、高齡開車視覺安全

### 高齡駕駛視覺安全文章
- **素材來源**：大叔提供鉅細靡遺研究版素材（交通部統計、115年換照新制、TPASS補助）
- **產出**：`content-planning/高齡駕駛視覺安全-115換照新制.html`
- **切角**：以驗光師視角出發，主攻視覺（白內障眩光、青光眼視野、糖尿病視網膜病變），帶入換照新制與 TPASS 配套為背景
- **標題**：115年高齡換照下修70歲！驗光師說：換照之前，先換眼睛的「行車執照」
- **包含**：HTML 全格式、FAQ 5題、Article + FAQPage schema
- **專家團隊評分**：調用「幹部聯席會議」（CTO、CSO、CMO）進行 93.6 分高分評價，並通過「頑固長輩」防禦力測試 (VERDICT: PASS)。
- **首圖優化**：生成原創「雨夜閉頂轎車」寫實首圖，並正式確立 `media/` 存放與英文命名規範。
- **規則更新**：已在 `AGENTS.md` 加入「🖼️ 圖片資源處理規範」，未來生成圖片將自動執行：英文命名 → 搬移至 media → 自動嵌入文案。
- **待辦**：Antigravity 完成圖文審核後，執行 `python check_and_publish.py` 自動發佈。

---

## 下午（續）— Blogger 自動發文 + 跨 Agent 協作介面

### Blogger 自動發文腳本
- `publish_to_blogger.py`：OAuth2 認證 + 讀取 HTML meta → 自動建草稿／發佈
- blog_id 已存入 `blog_id.txt`（uncle-glasses.net）
- token 已存入 `token.json`（之後不再需要登入）
- 測試：高齡駕駛文章草稿已成功送進 Blogger 後台

### 跨 Agent 協作介面（Claude Code ↔ Antigravity）
- **任務佇列**：`Inbox/待Antigravity圖文審核.md`（Claude Code 寫任務卡）
- **自動發佈偵測**：`check_and_publish.py`（掃描佇列，Antigravity 完成後自動發佈）
- **協作流程**：
  1. Claude Code 產 HTML → 寫任務卡（含圖片 prompt、標題待審）
  2. Antigravity 執行 `/title-optimizer` + 生圖 → 回填任務卡、更新 HTML
  3. Claude Code 執行 `python check_and_publish.py` → 自動送草稿

### 當日收尾
- 防重複發文機制：`publish_to_blogger.py` 新增 `find_existing_post()`，同標題草稿/發佈文已存在則自動中止
- `.gitignore` 補上 `credentials.json` / `token.json`（OAuth2 憑證保護）
- Blogger 草稿狀態：1 筆（ID: `4604546380070565529`），待手動填永久連結 `senior-driving-vision-safety` + 上傳首圖後發佈
- 圖片：`media/senior-driving-cataract-glare-night-rain.png`（Antigravity 生成，772KB），需手動上傳至 Blogger 圖片庫取得公開 URL
